/**
 * Must match `sedan-unified.svg` (`width` / `height` / `viewBox`). Damage fill toggles
 * on elements by `id` in that file — see `SedanUnifiedDiagram` + `sedanUnifiedPathIds`.
 */
export const SEDAN_LAYOUT_BASE_PX = { w: 276, h: 372 } as const;
